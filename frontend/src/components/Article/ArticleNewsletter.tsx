// Re-export the landing newsletter so the article page keeps the same
// subscribe block without duplicating markup.
export { Newsletter as ArticleNewsletter } from "@/components/Landing/Newsletter"
